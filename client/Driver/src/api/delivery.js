export const getDeliveryDetails = async (deliveryId) => {
  // Fetch delivery details from your backend
};

export const updateDeliveryStatus = async (deliveryId, status) => {
  // Update delivery status on your backend
};
